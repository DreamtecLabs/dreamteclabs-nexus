[Service]
# Nexus relies on Linux ping sockets for ICMP probes. The kernel policy is
# installed separately through sysctl, so the exporter does not need raw
# socket capabilities.
NoNewPrivileges=true
