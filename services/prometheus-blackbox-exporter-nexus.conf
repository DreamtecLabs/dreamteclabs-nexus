[Unit]
Requires=nexus-icmp-ping-policy.service
After=nexus-icmp-ping-policy.service

[Service]
# Nexus relies on Linux ping sockets for ICMP probes. The kernel policy is
# applied immediately before exporter startup by a root oneshot service, so
# the exporter itself does not need raw socket capabilities.
NoNewPrivileges=true
