[Service]
# Nexus uses the distro blackbox exporter for agentless ICMP probes. The
# exporter runs as the unprivileged prometheus user, so grant only the raw
# socket capability required by the ICMP prober.
AmbientCapabilities=CAP_NET_RAW
CapabilityBoundingSet=CAP_NET_RAW
NoNewPrivileges=true
