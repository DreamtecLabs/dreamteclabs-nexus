# Monitoring v1 scope

Monitoring v1 adds a Nexus-owned UI/API for agentless ICMP devices, a dedicated OpenTelemetry collector service, safe device lifecycle states, and authenticated SigNoz API connectivity. It does not yet create SigNoz alert rules automatically; that is intentionally deferred until the deployed SigNoz rule schema is validated.
