# Monitoring ownership boundary

Monitoring extensions remain under the existing Nexus seam: frontend code in `ui/src/nexus`, backend API code in `server/src/api/nexus`, and packaging/service integration in Nexus-owned service/debian files. No upstream PDM core module is repurposed as a telemetry backend.
