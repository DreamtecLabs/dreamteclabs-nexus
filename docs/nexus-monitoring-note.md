# Monitoring implementation note

The initial feature is intentionally narrow: Nexus-owned inventory/UI, centralized ICMP telemetry and real SigNoz API connectivity. Additional telemetry profiles and managed alert rules build on this foundation without requiring operators to edit collector targets in a terminal.
