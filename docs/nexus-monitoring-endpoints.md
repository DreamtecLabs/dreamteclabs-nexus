# Monitoring endpoints

The Nexus Monitoring API is exposed under `/monitoring` and is consumed by the authenticated Nexus UI. SigNoz remains behind the Nexus backend boundary so service-account credentials are never sent to the browser.
